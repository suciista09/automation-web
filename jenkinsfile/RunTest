pipeline {
    agent {
        node {
            label "docker-chrome"
        }
    }

    stages{
        stage('Preparation'){
            steps{
                sh 'echo preparation'
            }
        }

        stage('Run Test'){
            steps{
                sh './gradlew --continue clean cucumber --tags "@checkout" generateCucumberReports'
            }
        }

    }
}