pipeline {
    agent {
        node {
            label "docker-chrome"
        }
    }

    stages{

        stage('Dry Run Check'){
            Steps{
                ansiColor('xterm'){
                    sh './gradlew clean cucumber --cucumber-dry-run true'
                }
            }
        }

    }
}